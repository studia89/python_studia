def przemnoz_przez_dwa(liczby):
    for i in range(len(liczby)):
        liczby[i] = liczby[i] * 2
    return liczby

lista = [1, 2, 3, 4, 5]
wynik = przemnoz_przez_dwa(lista)
print(wynik)


o(n) = n

